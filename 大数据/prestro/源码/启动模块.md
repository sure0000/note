入口类： com.facebook.presto.server.PrestoServer

```java
// 模块加载
modules.add(
                new NodeModule(),           // 节点模块
                new DiscoveryModule(),      // 节点发现模块
                new HttpServerModule(),     // http server 模块
                new JsonModule(),           // Json 模块
                installModuleIf(
                        FeaturesConfig.class,
                        FeaturesConfig::isJsonSerdeCodeGenerationEnabled,
                        binder -> jsonBinder(binder).addModuleBinding().to(AfterburnerModule.class)),
                new SmileModule(),
                new JaxrsModule(true),
                new MBeanModule(),
                new JmxModule(),            // jmx 模块
                new JmxHttpModule(),
                new LogJmxModule(),
                new TraceTokenModule(),
                new JsonEventModule(),      // json 事件模块
                new HttpEventModule(),      // http 事件模块
                new ServerSecurityModule(), // 安全模块
                new AccessControlModule(),
                new EventListenerModule(),  // 事件监听模块
                new ServerMainModule(sqlParserOptions),
                new GracefulShutdownModule(),   // 关闭模块
                new WarningCollectorModule()
            );

modules.addAll(getAdditionalModules());
Bootstrap app = new Bootstrap(modules.build());     // 加载模块
...
Injector injector = app.strictConfig().initialize();
injector.getInstance(PluginManager.class).loadPlugins(); // 加载插件
injector.getInstance(StaticCatalogStore.class).loadCatalogs();  // 加载 catalog
```

加载模块配置文件：com.facebook.airlift.bootstrap.Bootstrap#initialize

```java
// Register configuration classes defined in the modules
configurationFactory.registerConfigurationClasses(modules);
```

com.facebook.airlift.bootstrap.LifeCycleManager#start